<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.chapter02.mapper.UserMapper">

    <select id="findById" parameterType="java.lang.Integer"
            resultType="com.example.entity.User">
        SELECT id, username, password, email, created_at, updated_at
        FROM `user` WHERE id = #{id}
    </select>

    <insert id="addUser" parameterType="com.example.entity.User"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO `user` (username, password, email)
        VALUES (#{username}, #{password}, #{email})
    </insert>

    <update id="updateUser" parameterType="com.example.entity.User">
        UPDATE `user`
        SET username = #{username}, password = #{password}, email = #{email}
        WHERE id = #{id}
    </update>

    <delete id="deleteUser" parameterType="java.lang.Integer">
        DELETE FROM `user` WHERE id = #{id}
    </delete>

    <select id="selectById" parameterType="java.lang.Integer"
            resultType="com.example.entity.User">
        SELECT id, username, password, email, created_at, updated_at
        FROM `user` WHERE id = #{id}
    </select>

    <insert id="insert" parameterType="com.example.entity.User"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO `user` (username, password, email)
        VALUES (#{username}, #{password}, #{email})
    </insert>

    <update id="update" parameterType="com.example.entity.User">
        UPDATE `user`
        SET username = #{username}, password = #{password}, email = #{email}
        WHERE id = #{id}
    </update>

    <delete id="deleteById" parameterType="java.lang.Integer">
        DELETE FROM `user` WHERE id = #{id}
    </delete>

    <resultMap id="userResultMap" type="com.example.chapter02.entity.Vo">
        <id property="p1" column="id"/>
        <result property="p2" column="username"/>
        <result property="p3" column="password"/>
    </resultMap>

    <select id="selectAllByVo" resultMap="userResultMap">
        SELECT id, username, password
        FROM `user` ORDER BY id
    </select>

    <select id="selectAll" resultType="com.example.entity.User">
        SELECT id AS uid, username, `password`, email
        FROM `user` ORDER BY id
    </select>

    <select id="selectAllVO" resultType="com.example.chapter02.entity.Vo">
        SELECT id AS p1, username AS p2, password AS p3
        FROM `user` ORDER BY id
    </select>
</mapper>
